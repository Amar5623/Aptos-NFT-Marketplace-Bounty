export interface MetricCardProps {
    title: string;
    value: string | number;
    icon: string;
  }
  
  export interface ChartCardProps {
    title: string;
    children: React.ReactNode;
  }
  
  